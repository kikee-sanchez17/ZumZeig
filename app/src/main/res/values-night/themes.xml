<!-- res/values-night/themes.xml -->
<resources>
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight">
        <!-- Configuración general del tema -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="imageSrc">@drawable/zumzeig</item>
        <item name="homeSrc">@drawable/homeiconwhite</item>
        <item name="calendarSrc">@drawable/calendariconwhite</item>
        <item name="savedSrc">@drawable/saveiconwhite</item>
        <item name="userSrc">@drawable/usericonwhite</item>
    </style>

    <!-- Estilo del botón para tema oscuro -->
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Estilo de los editText del login and sign up para tema oscuro -->
    <style name="EditTextStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxBackgroundMode">outline</item>
        <item name="boxStrokeColor">@color/white</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="boxStrokeWidthFocused">1dp</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="hintTextColor">@color/white</item>
        <item name="android:textCursorDrawable">@color/white</item>
        <item name="boxCornerRadiusBottomEnd">0dp</item>
        <item name="boxCornerRadiusBottomStart">0dp</item>
        <item name="boxCornerRadiusTopEnd">0dp</item>
        <item name="boxCornerRadiusTopStart">0dp</item>
    </style>

    <!-- Estilo de los titulos para tema oscuro -->
    <style name="Title1Style" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">50sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Estilo de los subtitulos para tema oscuro -->
    <style name="subtitleStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="android:textStyle">normal</item>
    </style>
</resources>
